<Window x:Class="TripleDetection.Presentation.Views.Production.ProductEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:TripleDetection.Presentation.Views.Production"
        Title="{Binding WindowTitle}" Height="450" Width="500"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="产品编码:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Code, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="产品名称:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="产品描述:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10" AcceptsReturn="True" TextWrapping="Wrap" Height="60"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="有效期类型:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <ComboBox Grid.Row="3" Grid.Column="1" ItemsSource="{Binding ValidTypes}" SelectedItem="{Binding ValidType}" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="4" Grid.Column="0" Text="有效期数量:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding ValidPeriod, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="5" Grid.Column="0" Text="方案文件:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <Grid Grid.Row="5" Grid.Column="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Text="{Binding SolFilePath, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="True"/>
            <Button Grid.Column="1" Content="浏览..." Width="60" Margin="5,0,0,0" Click="BtnBrowse_Click"/>
        </Grid>

        <TextBlock Grid.Row="6" Grid.Column="0" Text="状态:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <ComboBox Grid.Row="6" Grid.Column="1" ItemsSource="{Binding Statuses}" SelectedItem="{Binding Status}" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="7" Grid.Column="0" Text="" Margin="0"/>
        <TextBlock Grid.Row="7" Grid.Column="1" Text="{Binding ErrorMessage}" Foreground="Red" VerticalAlignment="Top" Margin="0,5,0,0"/>

        <StackPanel Grid.Row="8" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="保存" Width="80" Margin="5" Click="BtnSave_Click"/>
            <Button Content="取消" Width="80" Margin="5" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>