<UserControl x:Class="TripleDetection.Presentation.Views.Production.ProductListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:TripleDetection.Presentation.Views.Production"
             xmlns:vm="clr-namespace:TripleDetection.Presentation.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="White"
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="产品管理" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="10"/>

        <!-- Search Form -->
        <GroupBox Grid.Row="1" Header="查询条件" Margin="10,5" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="产品编码:" VerticalAlignment="Center" Margin="0,0,10,5" Foreground="{StaticResource TextPrimaryBrush}"/>
                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding QueryCode, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,5"/>

                <TextBlock Grid.Row="0" Grid.Column="2" Text="产品名称:" VerticalAlignment="Center" Margin="0,0,10,5" Foreground="{StaticResource TextPrimaryBrush}"/>
                <TextBox Grid.Row="0" Grid.Column="3" Text="{Binding QueryName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,5"/>

                <TextBlock Grid.Row="0" Grid.Column="4" Text="状态:" VerticalAlignment="Center" Margin="0,0,10,5" Foreground="{StaticResource TextPrimaryBrush}"/>
                <ComboBox Grid.Row="0" Grid.Column="5" SelectedValue="{Binding QueryStatus}" SelectedValuePath="Tag" Margin="0,0,10,5">
                    <ComboBoxItem Content="全部" Tag="{x:Null}"/>
                    <ComboBoxItem Content="启用" Tag="1"/>
                    <ComboBoxItem Content="停用" Tag="0"/>
                </ComboBox>

                <StackPanel Grid.Row="0" Grid.Column="6" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="查询" Width="80" Margin="5" Click="BtnSearch_Click"/>
                    <Button Content="重置" Width="80" Margin="5" Click="BtnReset_Click"/>
                </StackPanel>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="创建日期:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{StaticResource TextPrimaryBrush}"/>
                <DatePicker Grid.Row="1" Grid.Column="1" SelectedDate="{Binding QueryCreateAtFrom}" Margin="0,0,10,0"/>
                <TextBlock Grid.Row="1" Grid.Column="2" Text="至" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{StaticResource TextPrimaryBrush}"/>
                <DatePicker Grid.Row="1" Grid.Column="3" SelectedDate="{Binding QueryCreateAtTo}" Margin="0,0,10,0"/>
            </Grid>
        </GroupBox>

        <!-- Data List -->
        <Grid Grid.Row="2" Margin="10,5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,5">
                <Button Content="新增产品" Width="100" Click="BtnAdd_Click"/>
            </StackPanel>

            <DataGrid Grid.Row="1" ItemsSource="{Binding Products}" SelectedItem="{Binding SelectedProduct}"
                      AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                      SelectionMode="Single" HeadersVisibility="Column"
                      Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="序号" Binding="{Binding Id}" Width="60"/>
                    <DataGridTextColumn Header="产品编码" Binding="{Binding Code}" Width="120"/>
                    <DataGridTextColumn Header="产品名称" Binding="{Binding Name}" Width="150"/>
                    <DataGridTextColumn Header="有效期类型" Binding="{Binding ValidType}" Width="80"/>
                    <DataGridTextColumn Header="有效期" Binding="{Binding ValidPeriod}" Width="80"/>
                    <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="80"/>
                    <DataGridTextColumn Header="方案文件" Binding="{Binding SolFilePath}" Width="200"/>
                    <DataGridTextColumn Header="创建时间" Binding="{Binding CreateAt, StringFormat='yyyy-MM-dd HH:mm'}" Width="150"/>
                    <DataGridTemplateColumn Header="操作" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="编辑" Width="50" Margin="2" Click="BtnEdit_Click"/>
                                    <Button Content="删除" Width="50" Margin="2" Click="BtnDelete_Click"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <!-- Pagination -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="首页" Width="60" Margin="5" Click="BtnFirstPage_Click"/>
            <Button Content="上一页" Width="60" Margin="5" Click="BtnPrevPage_Click"/>
            <TextBlock Text="{Binding CurrentPageDisplay}" VerticalAlignment="Center" Margin="10"/>
            <Button Content="下一页" Width="60" Margin="5" Click="BtnNextPage_Click"/>
            <Button Content="末页" Width="60" Margin="5" Click="BtnLastPage_Click"/>
            <TextBlock Text="{Binding TotalPagesDisplay}" VerticalAlignment="Center" Margin="10"/>
        </StackPanel>
    </Grid>
</UserControl>