<Window x:Class="TripleDetection.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:TripleDetection"
        Title="Triple Detection - Phase 1 MVP"
        Height="750" Width="1284"
        WindowStartupLocation="CenterScreen"
        Background="#404040"
        Loaded="Window_Loaded"
        Closing="Window_Closing">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>  <!-- Header -->
            <RowDefinition Height="*"/>    <!-- Main content -->
            <RowDefinition Height="24"/> <!-- Status bar -->
        </Grid.RowDefinitions>

        <!-- Header Bar -->
        <Border Grid.Row="0" Background="#2D3748">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Left: Logo + System Name -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0">
                    <Border Width="32" Height="32" Background="#4FD1C5" CornerRadius="4">
                        <TextBlock Text="TD" Foreground="#1A202C" FontWeight="Bold"
                                   FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="Triple Detection" Foreground="White" FontSize="16" FontWeight="Bold"
                               VerticalAlignment="Center" Margin="12,0,0,0"/>
                </StackPanel>

                <!-- Right: User info + Logout -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0">
                    <TextBlock x:Name="txtCurrentUser" Text="Admin" Foreground="White"
                               VerticalAlignment="Center" Margin="0,0,16,0"/>
                    <Button x:Name="btnLogout" Content="退出" Click="BtnLogout_Click"
                            Background="#E53E3E" Foreground="White" Padding="12,6" BorderThickness="0" Cursor="Hand"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content Area with Navigation -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="navColumn" Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Navigation Rail -->
            <Border Grid.Column="0" Background="#1A202C">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Toggle Nav Button -->
                    <Button Grid.Row="0" x:Name="btnToggleNav" Content="◀" FontSize="12"
                            Style="{StaticResource NavButtonStyle}" HorizontalAlignment="Right"
                            Margin="0,8,8,0" Click="BtnToggleNav_Click" ToolTip="折叠导航"/>

                    <!-- Navigation Items -->
                    <StackPanel Grid.Row="1" x:Name="navPanel">
                        <Button x:Name="btnNavDashboard" Content="📊 仪表盘"
                                Style="{StaticResource NavButtonActiveStyle}" Click="BtnNav_Click" Tag="Dashboard"/>
                        <Button x:Name="btnNavDetection" Content="🔍 检测执行"
                                Style="{StaticResource NavButtonStyle}" Click="BtnNav_Click" Tag="Detection"/>
                        <Button x:Name="btnNavProducts" Content="📦 产品管理"
                                Style="{StaticResource NavButtonStyle}" Click="BtnNav_Click" Tag="Products"/>
                        <Button x:Name="btnNavTasks" Content="📋 任务管理"
                                Style="{StaticResource NavButtonStyle}" Click="BtnNav_Click" Tag="Tasks"/>
                        <Button x:Name="btnNavAudit" Content="📝 审计日志"
                                Style="{StaticResource NavButtonStyle}" Click="BtnNav_Click" Tag="Audit"/>
                        <Button x:Name="btnNavUsers" Content="👤 用户管理"
                                Style="{StaticResource NavButtonStyle}" Click="BtnNav_Click" Tag="Users"/>
                        <Button x:Name="btnNavSettings" Content="⚙️ 系统配置"
                                Style="{StaticResource NavButtonStyle}" Click="BtnNav_Click" Tag="Settings"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Main Content Region -->
            <Border Grid.Column="1" Background="#404040">
                <ContentControl x:Name="MainContentRegion"/>
            </Border>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="#2D3748">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" x:Name="txtStatus" Text="就绪" Foreground="#A0AEC0"
                           VerticalAlignment="Center" Margin="16,0" FontSize="11"/>
                <TextBlock Grid.Column="1" x:Name="txtTime" Text="" Foreground="#A0AEC0"
                           VerticalAlignment="Center" Margin="16,0" FontSize="11"/>
                <TextBlock x:Name="txtIOStatus" Text="IO: 未连接" Foreground="#A0AEC0"
                           VerticalAlignment="Center" Margin="16,0,0,0" FontSize="11"/>
            </Grid>
        </Border>
    </Grid>
</Window>