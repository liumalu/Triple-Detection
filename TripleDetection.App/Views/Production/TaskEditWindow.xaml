<Window x:Class="TripleDetection.Views.Production.TaskEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:TripleDetection.Views.Production"
        Title="{Binding WindowTitle}" Height="400" Width="500"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="任务名称:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="关联产品:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <ComboBox Grid.Row="1" Grid.Column="1" ItemsSource="{Binding Products}" SelectedValue="{Binding ProductId}" SelectedValuePath="Id" DisplayMemberPath="Name" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="生产日期:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <DatePicker Grid.Row="2" Grid.Column="1" SelectedDate="{Binding ProductionDate}" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="有效期至:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <DatePicker Grid.Row="3" Grid.Column="1" SelectedDate="{Binding ExpirationDate}" IsHitTestVisible="False" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="4" Grid.Column="0" Text="批次号:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding BatchNumber, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="5" Grid.Column="0" Text="" Margin="0"/>
        <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding ErrorMessage}" Foreground="Red" VerticalAlignment="Top" Margin="0,5,0,0"/>

        <StackPanel Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="保存" Width="80" Margin="5" Click="BtnSave_Click"/>
            <Button Content="取消" Width="80" Margin="5" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>