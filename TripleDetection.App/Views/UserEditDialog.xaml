<Window x:Class="TripleDetection.Views.UserEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="用户编辑" Height="360" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 用户名 -->
        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="用户名:" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4"/>
            <TextBox x:Name="txtUsername" IsEnabled="True"/>
        </StackPanel>

        <!-- 姓名 -->
        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <TextBlock Text="姓名:" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4"/>
            <TextBox x:Name="txtRealName"/>
        </StackPanel>

        <!-- 密码 -->
        <StackPanel Grid.Row="2" Margin="0,0,0,12">
            <TextBlock Text="密码:" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4"/>
            <PasswordBox x:Name="txtPassword"/>
        </StackPanel>

        <!-- 角色 -->
        <StackPanel Grid.Row="3" Margin="0,0,0,12">
            <TextBlock Text="角色:" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4"/>
            <ComboBox x:Name="cboRole">
                <ComboBoxItem Content="Admin" Tag="Admin"/>
                <ComboBoxItem Content="Supervisor" Tag="Supervisor"/>
                <ComboBoxItem Content="Operator" Tag="Operator" IsSelected="True"/>
            </ComboBox>
        </StackPanel>

        <!-- 按钮 -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="确定" Click="OkButton_Click" Width="80" Margin="0,0,8,0"
                    Style="{StaticResource PrimaryButtonStyle}"/>
            <Button Content="取消" Click="CancelButton_Click" Width="80"
                    Style="{StaticResource PrimaryButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>