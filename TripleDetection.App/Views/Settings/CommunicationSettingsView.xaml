<UserControl x:Class="TripleDetection.Views.Settings.CommunicationSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <TextBlock Text="通讯设置" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,20"/>

            <!-- 相机设置 -->
            <TextBlock Text="相机设置" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,12"/>
            <Border Background="{StaticResource CardBackgroundBrush}" Padding="16" Margin="0,0,0,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="相机IP:" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBox Grid.Row="0" Grid.Column="1" x:Name="txtCameraIp" Margin="0,0,0,8"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="相机端口:" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBox Grid.Row="1" Grid.Column="1" x:Name="txtCameraPort"/>
                </Grid>
            </Border>

            <!-- PLC设置 -->
            <TextBlock Text="PLC设置" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,12"/>
            <Border Background="{StaticResource CardBackgroundBrush}" Padding="16" Margin="0,0,0,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="PLC IP:" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBox Grid.Row="0" Grid.Column="1" x:Name="txtPlcIp" Margin="0,0,0,8"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="PLC端口:" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBox Grid.Row="1" Grid.Column="1" x:Name="txtPlcPort" Margin="0,0,0,8"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="PLC类型:" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <ComboBox Grid.Row="2" Grid.Column="1" x:Name="cboPlcType" Margin="0,0,0,8">
                        <ComboBoxItem Content="Mitsubishi" Tag="Mitsubishi"/>
                        <ComboBoxItem Content="Siemens" Tag="Siemens"/>
                        <ComboBoxItem Content="Omron" Tag="Omron"/>
                    </ComboBox>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="波特率:" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <ComboBox Grid.Row="3" Grid.Column="1" x:Name="cboBaudRate">
                        <ComboBoxItem Content="9600" Tag="9600"/>
                        <ComboBoxItem Content="19200" Tag="19200"/>
                        <ComboBoxItem Content="38400" Tag="38400"/>
                        <ComboBoxItem Content="57600" Tag="57600"/>
                        <ComboBoxItem Content="115200" Tag="115200" IsSelected="True"/>
                    </ComboBox>
                </Grid>
            </Border>

            <Button Content="保存设置" Click="SaveButton_Click" Style="{StaticResource PrimaryButtonStyle}" HorizontalAlignment="Left"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>