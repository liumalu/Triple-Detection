<UserControl x:Class="TripleDetection.Views.UserManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <TextBlock Grid.Row="0" Text="用户权限管理" FontSize="18" FontWeight="Bold"
                   Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,20"/>

        <!-- 查询条件 -->
        <Border Grid.Row="1" Background="{StaticResource CardBackgroundBrush}" Padding="16" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <StackPanel Orientation="Horizontal" Margin="0,0,16,0">
                    <TextBlock Text="用户名:" VerticalAlignment="Center" Margin="0,0,8,0"
                               Foreground="{StaticResource TextPrimaryBrush}"/>
                    <TextBox x:Name="txtSearchUsername" Width="120" VerticalContentAlignment="Center" Padding="4,2"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,0,16,0">
                    <TextBlock Text="角色:" VerticalAlignment="Center" Margin="0,0,8,0"
                               Foreground="{StaticResource TextPrimaryBrush}"/>
                    <ComboBox x:Name="cboSearchRole" Width="120" VerticalContentAlignment="Center">
                        <ComboBoxItem Content="" IsSelected="True"/>
                        <ComboBoxItem Content="Admin"/>
                        <ComboBoxItem Content="Supervisor"/>
                        <ComboBoxItem Content="Operator"/>
                        <ComboBoxItem Content="Viewer"/>
                    </ComboBox>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,0,16,0">
                    <TextBlock Text="状态:" VerticalAlignment="Center" Margin="0,0,8,0"
                               Foreground="{StaticResource TextPrimaryBrush}"/>
                    <ComboBox x:Name="cboSearchStatus" Width="120" VerticalContentAlignment="Center">
                        <ComboBoxItem Content="" IsSelected="True"/>
                        <ComboBoxItem Content="正常"/>
                        <ComboBoxItem Content="已禁用"/>
                        <ComboBoxItem Content="已锁定"/>
                    </ComboBox>
                </StackPanel>

                <Button Content="查询" Width="80" Margin="5" Click="BtnSearch_Click"
                        Style="{StaticResource PrimaryButtonStyle}"/>
                <Button Content="重置" Width="80" Margin="5" Click="BtnReset_Click"/>
            </StackPanel>
        </Border>

        <!-- 数据列表 -->
        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 工具栏：新增按钮右对齐 -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,5">
                <Button Content="新增用户" Width="100" Click="BtnAdd_Click"
                        Style="{StaticResource PrimaryButtonStyle}"/>
            </StackPanel>

            <!-- 用户列表 -->
            <Border Grid.Row="1" Background="{StaticResource CardBackgroundBrush}" Padding="16">
                <DataGrid x:Name="dgUsers" AutoGenerateColumns="False"
                          ItemsSource="{Binding Users}"
                          SelectionMode="Single"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          IsReadOnly="True"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          Background="White"
                          BorderThickness="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="用户名" Binding="{Binding Username}" Width="120"/>
                        <DataGridTextColumn Header="姓名" Binding="{Binding RealName}" Width="100"/>
                        <DataGridTextColumn Header="角色" Binding="{Binding Role}" Width="80"/>
                        <DataGridTextColumn Header="状态" Binding="{Binding StatusText}" Width="80"/>
                        <DataGridTextColumn Header="创建时间" Binding="{Binding CreateAt, StringFormat=yyyy-MM-dd HH:mm}" Width="140"/>
                        <DataGridTemplateColumn Header="操作" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="编辑" Width="50" Margin="2" Click="BtnEdit_Click"/>
                                        <Button Content="删除" Width="50" Margin="2" Click="BtnDelete_Click"
                                                Style="{StaticResource DangerButtonStyle}"/>
                                        <Button Content="启用" Width="50" Margin="2" Click="BtnEnable_Click"/>
                                        <Button Content="禁用" Width="50" Margin="2" Click="BtnDisable_Click"/>
                                        <Button Content="锁定" Width="50" Margin="2" Click="BtnLock_Click"/>
                                        <Button Content="解锁" Width="50" Margin="2" Click="BtnUnlock_Click"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>

        <!-- 分页 -->
        <Border Grid.Row="3" Background="{StaticResource CardBackgroundBrush}" Padding="10" Margin="0,10,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="首页" Width="60" Margin="5" Click="BtnFirstPage_Click"/>
                <Button Content="上一页" Width="60" Margin="5" Click="BtnPreviousPage_Click"/>
                <TextBlock Text="{Binding CurrentPageDisplay}" VerticalAlignment="Center" Margin="10,0"/>
                <Button Content="下一页" Width="60" Margin="5" Click="BtnNextPage_Click"/>
                <Button Content="末页" Width="60" Margin="5" Click="BtnLastPage_Click"/>
                <TextBlock Text="{Binding TotalPagesDisplay}" VerticalAlignment="Center" Margin="10,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>