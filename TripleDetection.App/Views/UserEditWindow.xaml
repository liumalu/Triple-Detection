<Window x:Class="TripleDetection.Views.UserEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding WindowTitle}" Height="380" Width="450"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="用户名:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10" IsEnabled="{Binding IsEditMode, Converter={StaticResource InverseBoolConverter}}"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="真实姓名:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding RealName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="密码:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <PasswordBox Grid.Row="2" Grid.Column="1" x:Name="txtPassword" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="角色:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <ComboBox Grid.Row="3" Grid.Column="1" ItemsSource="{Binding Roles}" SelectedItem="{Binding Role}" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="4" Grid.Column="0" Text="启用:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <CheckBox Grid.Row="4" Grid.Column="1" IsChecked="{Binding IsEnabled}" VerticalAlignment="Center" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="5" Grid.Column="0" Text="" Margin="0"/>
        <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding ErrorMessage}" Foreground="Red" VerticalAlignment="Top" Margin="0,5,0,0"/>

        <StackPanel Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="保存" Width="80" Margin="5" Click="BtnSave_Click"/>
            <Button Content="取消" Width="80" Margin="5" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>