<UserControl x:Class="TripleDetection.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:TripleDetection.ViewModels.Settings;assembly=TripleDetection.App"
             Background="White">

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 左侧分类导航 -->
        <Border Grid.Column="0" Background="{StaticResource CardBackgroundBrush}" Margin="0,0,16,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0">
                    <Button x:Name="btnCommunication" Content="通讯设置"
                            Click="NavButton_Click" Tag="Communication"
                            Style="{StaticResource NavButtonActiveStyle}"/>
                    <Button x:Name="btnVmSettings" Content="VisionMaster设置"
                            Click="NavButton_Click" Tag="VmSettings"
                            Style="{StaticResource NavButtonStyle}"/>
                    <Button x:Name="btnSystem" Content="系统设置"
                            Click="NavButton_Click" Tag="System"
                            Style="{StaticResource NavButtonStyle}"/>
                    <Button x:Name="btnDeviceControl" Content="设备控制设置"
                            Click="NavButton_Click" Tag="DeviceControl"
                            Style="{StaticResource NavButtonStyle}"/>
                </StackPanel>

                <!-- 同步到VM按钮 -->
                <Button Grid.Row="1" Content="同步到VM" Margin="8,16,8,8"
                        Click="SyncButton_Click"
                        Style="{StaticResource PrimaryButtonStyle}"/>
            </Grid>
        </Border>

        <!-- 右侧内容区 -->
        <ContentControl Grid.Column="1" x:Name="ContentArea"/>
    </Grid>
</UserControl>