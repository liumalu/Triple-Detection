<UserControl x:Class="TripleDetection.Views.App.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <TextBlock Grid.Row="0" Text="仪表盘" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,20"/>

        <!-- 统计卡片 -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <Border Background="{StaticResource CardBackgroundBrush}" Padding="20" Margin="0,0,16,0" MinWidth="150">
                <StackPanel>
                    <TextBlock Text="今日OK" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
                    <TextBlock x:Name="txtTodayOk" Text="0" Foreground="#00C000" FontSize="32" FontWeight="Bold"/>
                </StackPanel>
            </Border>
            <Border Background="{StaticResource CardBackgroundBrush}" Padding="20" Margin="0,0,16,0" MinWidth="150">
                <StackPanel>
                    <TextBlock Text="今日NG" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
                    <TextBlock x:Name="txtTodayNg" Text="0" Foreground="#FF0000" FontSize="32" FontWeight="Bold"/>
                </StackPanel>
            </Border>
            <Border Background="{StaticResource CardBackgroundBrush}" Padding="20" Margin="0,0,16,0" MinWidth="150">
                <StackPanel>
                    <TextBlock Text="总任务数" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
                    <TextBlock x:Name="txtTotalTasks" Text="0" Foreground="{StaticResource TextPrimaryBrush}" FontSize="32" FontWeight="Bold"/>
                </StackPanel>
            </Border>
            <Border Background="{StaticResource CardBackgroundBrush}" Padding="20" MinWidth="150">
                <StackPanel>
                    <TextBlock Text="待审核" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
                    <TextBlock x:Name="txtPending" Text="0" Foreground="#ED8936" FontSize="32" FontWeight="Bold"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- 最近检测列表 -->
        <Border Grid.Row="2" Background="{StaticResource CardBackgroundBrush}" Padding="16">
            <StackPanel>
                <TextBlock Text="最近检测" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,12"/>
                <DataGrid x:Name="dgRecent" AutoGenerateColumns="False" Background="Transparent"
                          Foreground="{StaticResource TextPrimaryBrush}" BorderThickness="1,1,1,1" BorderBrush="{StaticResource BorderBrush}" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="时间" Binding="{Binding DetectionTime}" Width="150"/>
                        <DataGridTextColumn Header="任务" Binding="{Binding TaskName}" Width="*"/>
                        <DataGridTextColumn Header="结果" Binding="{Binding Result}" Width="80"/>
                        <DataGridTextColumn Header="置信度" Binding="{Binding Confidence}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Border>

        <!-- 快捷操作 -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,16,0,0">
            <Button Content="新建检测" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,8,0"/>
            <Button Content="管理产品" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,8,0"/>
            <Button Content="查看任务" Style="{StaticResource PrimaryButtonStyle}"/>
        </StackPanel>
    </Grid>
</UserControl>